// Feature flags (client-side)
export const FEATURES = {
  // Use simple conversation-based E2E instead of complex Matrix Olm
  e2e_text_simple: true,
  // Reduce console noise unless explicitly enabled
  debug_messaging: true, // Temporarily disabled to reduce console spam
};


